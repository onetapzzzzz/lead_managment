# Lead Exchange - Система управления лидами

Современная платформа для обмена лидами с Telegram-ботом, веб-приложением и системой поинтов.

## 📋 Описание

Lead Exchange — это полнофункциональная система управления лидами, которая позволяет пользователям:
- 📤 Загружать лиды и получать поинты при их продаже
- 🛒 Покупать лиды на маркетплейсе
- 💰 Управлять балансом поинтов
- 📊 Отслеживать статистику и историю транзакций

### Ключевые особенности

- 🎨 **Современный UI** — адаптивный дизайн с поддержкой светлой и тёмной темы
- 📱 **Mobile-first** — оптимизировано для мобильных устройств (360-430px)
- ✨ **Плавные анимации** — Framer Motion для улучшения UX
- 🤖 **Telegram Bot** — удобный интерфейс через Telegram
- 💎 **Система поинтов** — уникальная экономика с дробными поинтами
- 🔒 **Антифрод защита** — проверка дубликатов и свежести лидов

## 🏗️ Архитектура

Проект состоит из двух основных компонентов:

1. **Frontend (Next.js)** — веб-приложение с API routes
2. **Telegram Bot (Python)** — бот для работы через Telegram

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ и npm
- Python 3.9+
- PostgreSQL (или SQLite для разработки)
- Telegram Bot Token

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/onetapzzzzz/lead_managment.git
cd lead_managment
```

2. **Установите зависимости фронтенда:**
```bash
npm install
```

3. **Установите зависимости бота:**
```bash
cd bot
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
cd ..
```

4. **Настройте переменные окружения:**

Создайте файл `.env.local` в корне проекта:
```env
# База данных
DATABASE_URL="postgresql://leads_user:leads_password@localhost:5432/leads_db"
# или для SQLite: DATABASE_URL="file:./dev.db"

# Telegram Bot
TELEGRAM_BOT_TOKEN="your_bot_token_here"
WEB_APP_URL="http://localhost:3000"
```

Создайте файл `bot/.env`:
```env
TELEGRAM_BOT_TOKEN="your_bot_token_here"
WEB_APP_URL="http://localhost:3000"
DATABASE_URL="postgresql://leads_user:leads_password@localhost:5432/leads_db"
BOT_API_PORT=8001
```

5. **Настройте базу данных:**
```bash
# Запустите PostgreSQL через Docker
docker-compose up -d

# Или используйте SQLite (измените DATABASE_URL в .env.local)
# Создайте миграции
npx prisma migrate dev

# (Опционально) Заполните тестовыми данными
npm run seed
```

6. **Запустите приложение:**

В первом терминале (Next.js):
```bash
npm run dev
```

Во втором терминале (Telegram Bot):
```bash
cd bot
source venv/bin/activate  # На Windows: venv\Scripts\activate
python main.py
```

Приложение будет доступно на [http://localhost:3000](http://localhost:3000)

## 📁 Структура проекта

```
leads/
├── src/
│   ├── app/              # Next.js страницы и API routes
│   │   ├── api/          # API endpoints
│   │   ├── leads/        # Страницы лидов
│   │   ├── market/       # Маркетплейс
│   │   └── ...
│   ├── components/       # React компоненты
│   ├── hooks/            # Custom React hooks
│   ├── lib/              # Утилиты и хелперы
│   └── contexts/         # React контексты
├── bot/                  # Python Telegram бот
│   ├── main.py          # Основной файл бота
│   ├── api_server.py     # HTTP API сервер
│   └── requirements.txt # Python зависимости
├── prisma/              # Prisma схемы и миграции
│   └── schema.prisma    # Схема базы данных
└── docker-compose.yml   # Docker конфигурация для PostgreSQL
```

## 🎯 Основной функционал

### Система поинтов

- Поинты нельзя купить — только заработать загружая лиды
- При продаже лида владелец получает поинты:
  - 1-я покупка: **1.0 поинт**
  - 2-я покупка: **0.7 поинтов**
  - 3-я покупка: **0.3 поинта**
  - Максимум: **2.0 поинта** за лид

### Загрузка лидов

- Парсинг телефонов из текста (автоматическое извлечение)
- Валидация формата (+7XXXXXXXXXX)
- Проверка на дубликаты
- Антифрод защита (нельзя загружать свежие лиды повторно)
- Пакетная загрузка с модерацией

### Маркетплейс

- Покупка лидов за поинты
- Фильтрация по нише и региону
- Отображение статуса (уникальный, куплен 1 раз, куплен 2 раза)
- История покупок

### Telegram Bot

**Команды:**
- `/start` — регистрация и главное меню
- `/help` — справка
- `/stats` — статистика пользователя
- `/balance` — баланс поинтов

**Функции:**
- Открытие веб-приложения через Telegram WebApp
- Уведомления о загрузке и покупке лидов
- Быстрый доступ к основным функциям

## 🗄️ База данных

Проект использует Prisma ORM. Основные модели:

- **User** — пользователи системы
- **Lead** — лиды (телефоны с метаданными)
- **LeadPurchase** — история покупок лидов
- **LeadUploadBatch** — пакеты загрузки лидов
- **Transaction** — транзакции поинтов

Подробнее в `prisma/schema.prisma`

## 🛠️ Технологии

### Frontend
- **Next.js 15.1.6** — React фреймворк
- **TypeScript** — типизация
- **Tailwind CSS** — стилизация
- **Framer Motion** — анимации
- **React Query** — управление состоянием
- **Zod** — валидация данных

### Backend
- **Prisma** — ORM для работы с БД
- **PostgreSQL/SQLite** — база данных
- **Next.js API Routes** — REST API

### Telegram Bot
- **Python 3.9+**
- **aiogram 2.25.2** — Telegram Bot API
- **httpx** — HTTP клиент
- **python-dotenv** — управление переменными окружения

## 📝 Скрипты

```bash
# Разработка
npm run dev          # Запуск Next.js dev сервера
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен сервера

# База данных
npx prisma migrate dev    # Создать и применить миграцию
npx prisma studio         # Открыть Prisma Studio
npm run seed              # Заполнить БД тестовыми данными

# Линтинг
npm run lint              # Проверка кода
```

## 🔧 Конфигурация

### Переменные окружения

**`.env.local` (Next.js):**
- `DATABASE_URL` — URL подключения к БД
- `TELEGRAM_BOT_TOKEN` — токен Telegram бота
- `WEB_APP_URL` — URL веб-приложения

**`bot/.env` (Python Bot):**
- `TELEGRAM_BOT_TOKEN` — токен Telegram бота
- `WEB_APP_URL` — URL веб-приложения
- `DATABASE_URL` — URL подключения к БД
- `BOT_API_PORT` — порт для API сервера (по умолчанию 8001)

## 🚢 Развертывание

### Продакшен

1. **Настройте переменные окружения** для продакшена
2. **Соберите Next.js приложение:**
```bash
npm run build
npm run start
```

3. **Запустите Telegram Bot:**
```bash
cd bot
python api_server.py  # Запуск API сервера
python main.py        # Запуск бота
```

4. **Настройте webhook** (опционально):
```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://your-domain.com/api/bot/webhook"
```

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект находится в разработке.

## 👤 Автор

**onetapzzzzz**

- GitHub: [@onetapzzzzz](https://github.com/onetapzzzzz)

## 🙏 Благодарности

- Next.js команде за отличный фреймворк
- aiogram за удобную библиотеку для Telegram ботов
- Prisma за мощный ORM

---

⭐ Если проект был полезен, поставьте звезду!
